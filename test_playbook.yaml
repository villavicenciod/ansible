---
-
  hosts: all
  name: "test ansible"
  strategy: free
  tasks:
      -
        name: Run some command
        command: /bin/whoami
        ignore_errors: true
        register: command_output1
      -
        debug:
          var: command_output1
        name: Command 1 output

